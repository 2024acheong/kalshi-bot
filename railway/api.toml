[build]
builder = "NIXPACKS"
buildCommand = "python -m pip install -U pip && python -m pip install -e packages/core -e apps/api"
watchPatterns = [
  "apps/api/**",
  "packages/core/**",
  "pyproject.toml",
]

[deploy]
startCommand = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
