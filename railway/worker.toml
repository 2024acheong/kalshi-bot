[build]
builder = "NIXPACKS"
buildCommand = "pip install -U pip && pip install -e packages/core -e services/worker -e services/models"
watchPatterns = [
  "services/worker/**",
  "services/models/**",
  "packages/core/**",
  "pyproject.toml",
]

[deploy]
startCommand = "python -m worker.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
